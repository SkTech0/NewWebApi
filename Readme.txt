✅ Git & .NET Setup Commands:

git status                         # Check current git status
git init                           # Initialize a new git repository

dotnet new gitignore               # Create a .gitignore file using dotnet template
dotnet new globaljson              # Create a global.json file

✍️ Sample Commit Message:

git add .                          # Stage all changes
git commit -m "Initialize repository with .gitignore and global.json"

You can customize the commit message depending on what files or features you've added. Example:
"Initial commit: setup .NET project structure and gitignore"


🔀 Branch & Remote Setup:

git branch                         # Check current branch
git branch -M main                 # Rename the current branch to 'main'
git branch                         # Confirm that 'main' is now the branch

git remote add origin "https://github.com/username/repo.git"  # Add remote repo
git push -u origin main            # Push code to GitHub and set upstream













git status
git init

dotnet new gitignore
dotnet new globaljson

write message for for Commit

stage changes 

then Commit

git branch

git branch -M main 
git branch

git remote add origin "url of repository"
 
git push -u origin main
